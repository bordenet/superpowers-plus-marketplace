# Marketplace v2.6.0 Sync Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bring superpowers-plus-marketplace README and marketplace.json in sync with superpowers-plus v2.6.0, which folded all obra/superpowers skills in and now ships 95 skills.

**Architecture:** Two file edits in the marketplace repo. No code, no tests — documentation and JSON catalog only. Both edits are independent and can be done in either order.

**Tech Stack:** Markdown, JSON, git.

---

## Files Changed

- Modify: `.claude-plugin/marketplace.json` — remove stale `superpowers` plugin entry
- Modify: `README.md` — remove prerequisite section, update counts/domains, fix Quick Start

---

### Task 1: Update marketplace.json — remove stale `superpowers` entry

**Context:** v2.6.0 folded all 14 obra/superpowers skills directly into superpowers-plus. The `superpowers` plugin entry is now redundant and misleading — users who follow the marketplace will install skills they already have, and may see conflicts.

**Files:**
- Modify: `.claude-plugin/marketplace.json`

- [ ] **Step 1: Verify current state**

Run:
```bash
cat .claude-plugin/marketplace.json
```
Expected: two entries in `plugins[]` — `superpowers` and `superpowers-plus`.

- [ ] **Step 2: Remove the `superpowers` plugin entry and update metadata description**

Replace the file with:
```json
{
  "name": "superpowers-plus-marketplace",
  "owner": {
    "name": "Matt Bordenet",
    "email": "bordenet@users.noreply.github.com"
  },
  "metadata": {
    "description": "95 skills across 9 domains: engineering, productivity, wiki, writing, observability, issue-tracking, security, research, experimental",
    "version": "1.0.0"
  },
  "plugins": [
    {
      "name": "superpowers-plus",
      "source": {
        "source": "url",
        "url": "https://github.com/bordenet/superpowers-plus.git"
      },
      "description": "95 skills across 9 domains: engineering gates, wiki pipeline, issue tracking, security audits, AI slop detection, productivity orchestration",
      "version": "2.6.0",
      "strict": true
    }
  ]
}
```

- [ ] **Step 3: Verify JSON is valid**

Run:
```bash
python3 -m json.tool .claude-plugin/marketplace.json > /dev/null && echo "valid"
```
Expected: `valid`

- [ ] **Step 4: Commit**

```bash
git add .claude-plugin/marketplace.json
git commit -m "chore: remove stale superpowers prerequisite entry (folded into v2.6.0)"
```

---

### Task 2: Update README.md — remove prerequisite section, fix counts and domains

**Context:** The README has four stale areas:
1. Header line 3 describes the project with outdated/vague domain list
2. Line 5 credits obra/superpowers as the base — now it's bordenet/superpowers (maintained fork)
3. The entire `### superpowers (Prerequisite)` section (lines 17–34) is obsolete
4. superpowers-plus "What you get" lists 47 skills and mentions TypeScript as a domain (no such domain exists)
5. Quick Start installs both plugins; now only superpowers-plus is needed
6. Support links include obra/superpowers — no longer the direct dependency

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Rewrite README.md with all fixes applied**

Replace the entire file with:

```markdown
# superpowers-plus-marketplace

Claude Code plugin marketplace for [superpowers-plus](https://github.com/bordenet/superpowers-plus) — 95 skills across engineering, wiki, issue tracking, security, writing, productivity, observability, research, and experimental domains.

Built on [bordenet/superpowers](https://github.com/bordenet/superpowers), a maintained fork of obra/superpowers by Jesse Vincent.

## Installation

Add this marketplace to Claude Code:

\`\`\`bash
/plugin marketplace add bordenet/superpowers-plus-marketplace
\`\`\`

## Available Plugins

### superpowers-plus

**Description:** 95 skills across 9 domains that enforce the practices AI coding assistants otherwise skip: gates, review pipelines, investigation protocols, and quality loops.

**Install:**
\`\`\`bash
/plugin install superpowers-plus@superpowers-plus-marketplace
\`\`\`

**What you get:**
- 95 skills across 9 domains
- Engineering (38): commit gates, code review battery, TDD, debugging, feature development, debate, branch conventions
- Productivity (20): thinking-orchestrator, adversarial-search, think-twice, todo management, skill authoring
- Wiki (9): orchestrator pipeline, verify, refactor, content coherence, secret audit, link verification
- Writing (8): AI slop detection/elimination, professional language audit, markdown discipline
- Observability (8): evolution loop, exhaustive audit, completeness checks, holistic repo verification
- Issue Tracking (6): authoring, editing, link verification, comment debunker
- Security (4): repo security scan, public-repo IP audit, wiki instruction guard, security upgrade
- Research (3): Perplexity integration, expert interviewer, incorporating research
- Experimental (1): self-prompting

**Repository:** https://github.com/bordenet/superpowers-plus

---

## Quick Start

\`\`\`bash
/plugin marketplace add bordenet/superpowers-plus-marketplace
/plugin install superpowers-plus@superpowers-plus-marketplace
\`\`\`

Restart Claude Code, then ask:
- "what skills do I have" — lists all available skills
- "help me write a wiki page" — triggers wiki-orchestrator pipeline
- "check this code for secrets" — triggers repo-security-scan skill
- "review this code" or `/sp-cr-battery` — dispatches 5 parallel specialist reviewers

## Marketplace Structure

\`\`\`
superpowers-plus-marketplace/
├── .claude-plugin/
│   └── marketplace.json    # Plugin catalog
├── README.md               # This file
└── LICENSE                 # MIT License
\`\`\`

## Support

- **Issues:** https://github.com/bordenet/superpowers-plus-marketplace/issues
- **superpowers-plus:** https://github.com/bordenet/superpowers-plus
- **bordenet/superpowers (base):** https://github.com/bordenet/superpowers

## License

Marketplace metadata: MIT License

Individual plugins retain their own licenses.
```

- [ ] **Step 2: Verify skill count claim matches reality**

Run:
```bash
find /Users/matt/git/Personal/superpowers-plus/skills -name "skill.md" | wc -l
```
Expected: `95` — confirms the count in the README is accurate.

- [ ] **Step 3: Verify domain list matches reality**

Run:
```bash
find /Users/matt/git/Personal/superpowers-plus/skills -mindepth 2 -maxdepth 2 -type d | sort | awk -F/ '{domain=$(NF-1); skill=$NF; counts[domain]++} END {for (d in counts) print counts[d], d}' | sort -rn
```
Expected output matches the README's per-domain counts:
```
38 engineering
20 productivity
9  wiki
8  writing
8  observability
6  issue-tracking
4  security
3  research
1  experimental
```

- [ ] **Step 4: Commit**

```bash
git add README.md
git commit -m "docs: sync README with superpowers-plus v2.6.0 (95 skills, drop obra prerequisite)"
```

---

## Self-Review

**Spec coverage:**
- ✅ Skill count 47 → 95
- ✅ Removed `superpowers` prerequisite section from README
- ✅ Removed `superpowers` entry from marketplace.json
- ✅ Quick Start simplified from 3 commands to 2
- ✅ Domain list corrected (no TypeScript domain, accurate per-domain counts)
- ✅ Base repo credit updated to bordenet/superpowers
- ✅ Support links updated

**Placeholder scan:** No TBD, TODO, or fill-in-later language present.

**Type consistency:** N/A — no code.
